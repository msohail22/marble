{
  "workspace": [
    "./apps/control-plane",
    "./apps/dashboard",
    "./apps/api",
    "./apps/web",
    "./packages/types"
  ],
  "tasks": {
    "control-plane": "deno task --cwd apps/control-plane start",
    "dashboard": "deno task --cwd apps/dashboard start",
    "api": "deno task --cwd apps/api dev",
    "web": "deno task --cwd apps/web dev"
  }
}

