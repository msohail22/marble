apiVersion: v1
kind: Service
metadata:
  name: marble-web-service
  namespace: marble
  labels:
    app.kubernetes.io/name: marble-web
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    name: http
  selector:
    app: marble-web

