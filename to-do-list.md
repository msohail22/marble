- static, relative, absolute, fixed what are all these present in css sikhna ha
- absolute and bottom-6 react me matlab kaise interlinked hai ?, check authfooter fie for this here 
- &copy matlab @ symbol hota hai html jargon me 
- text-muted-foreground matlab ki jo text hai use parent component ka muted color hai kya, mujhe ye nahi maloom
- default export matlab only one per file and you can import without using braces here, if not then need to use {} this symbol to import here 
- reactnode type, it can return anything that a react fn returns, so just keep it as a react node type if no type is specified here 
- need to add css to this nopermissionstate.tsx file present here
- import { createPortal } from "react-dom"; it is used to like keep elements in different places than the actual html present there, also like 
  it is expecially useful when we want to build modals, so this is required to be kept here because if we just keep modal in the root div then other elements may interfere with that here and may cause problems like modal would cover the entire screen or like modal won't be visible above all so like we use it to keep it in a different div container 
- so like docker build -f ui/Dockerfile -t marble-web . , here we need to . so that the docker knows what is the build context, so like build context is like which directories is docker allowed to access here
